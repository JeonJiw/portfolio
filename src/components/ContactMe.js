import { forwardRef } from "react";

export const ContactMe = forwardRef((props, ref) => {
  return (
    <section id="contactme" ref={ref}>
      {/* Your ContactMe content */}
    </section>
  );
});
